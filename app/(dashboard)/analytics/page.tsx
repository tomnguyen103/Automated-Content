import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function AnalyticsPage() {
  return (
    <PlaceholderPage
      title="Analytics"
      description="Measure publishing volume, agent activity, platform performance, reply automation, and usage."
      phase="Phase 8"
      tabs={["Overview", "Platforms", "Content", "Replies", "Usage"]}
    />
  );
}
